<Window x:Class="Dapper框架使用Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dapper框架使用Demo"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <DataGrid x:Name="dataGrid" AutoGenerateColumns="False" HorizontalAlignment="Left" Margin="48,38,0,0" VerticalAlignment="Top" Height="228" Width="445">
            <DataGrid.Columns>
                <DataGridTextColumn Header="用户名" Width="80"  Binding="{Binding  FUserName}"/>
                <DataGridTextColumn Header="密码"   Width="80"  Binding="{Binding FPassword}"/>
                <DataGridTextColumn Header="数据地址"  Width="400"  Binding="{Binding ApiUrl}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="button" Content="Button" HorizontalAlignment="Left" Margin="25,13,0,0" VerticalAlignment="Top" Width="75" Click="button_Click"/>
    </Grid>
</Window>
